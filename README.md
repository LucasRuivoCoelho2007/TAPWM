# Instruções

Comandos para criar a pasta `node_modules` corretamente

```bash
npm install express --save
npm install cors --save
```
Obs: crie o .env para colocar sua senha (use o .env.example para isso)

e também mude os dados de acordo com o seu banco de dados no arquivo `app/config/database`